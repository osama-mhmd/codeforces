#include <iostream>
#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main()
{
  long int t;
  cin >> t;

  while (t--)
  {
    ll n, l, r;
    cin >> n >> l >> r;

    ll a[n];

    if (r - l + 1 < n)
    {
      cout << "NO" << endl;
    }
    else
    {
      for (ll i = l; i < r; i++)
      {
        if ()
      }
    }
  }

  return 0;
}